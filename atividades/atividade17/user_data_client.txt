#!/bin/bash

export usuario=alunoufc
export senha=123scripts789
export server_ip=172.31.22.209

sudo apt update
sudo apt install mysql-client -y

cd /home/ubuntu

cat << EOF > .my.cnf
[client]
user=$usuario
password=$senha
EOF

echo $server_ip > output

mysql -u $usuario scripts -h $server_ip -e "USE scripts; CREATE TABLE Teste ( atividade INT );"
